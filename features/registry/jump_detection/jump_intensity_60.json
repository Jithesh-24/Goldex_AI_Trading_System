{
  "feature_id": "jump_intensity_60",
  "family": "jump_detection",
  "mathematical_definition": "rolling(60).sum() of the is_jump indicator, where is_jump = |ret1 * close| > 3 * clip(ewma_vol * close, 1e-9, None) (count of price jumps exceeding 3x local price-space volatility over the trailing 60 bars)",
  "source_module": "features.jump_detection.compute_jump_detection",
  "required_state": [
    "close"
  ],
  "update_trigger": "M1_CLOSE",
  "window": 60,
  "causal": true,
  "live_compatible": true,
  "computational_cost": "LOW",
  "missing_value_policy": "NaN for windows with fewer than 60 observed bars, never zero-filled",
  "warmup_bars": 60,
  "dependencies": [],
  "historical_coverage": "FULL_HISTORY",
  "status": "REDUNDANT",
  "status_reason": "Not a v3 survivor (research/output/v3_feature_survivors.json): catboost_importance=0.081 (<0.3), MI rank 40 (outside top 15). catboost_importance=0.081, mi_rank=40, family=C_jump_change. Kept registered per spec section 9 -- Phase 4 may find it useful for a different target.",
  "evidence_ref": "research/output/v3_feature_survivors.json",
  "version": "v1"
}
