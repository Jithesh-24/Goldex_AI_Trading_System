{
  "feature_id": "activity_state",
  "family": "regime_state",
  "mathematical_definition": "causal_tercile(tick_volume_zscore_60, 500) -- rolling (trailing, shift(1)) tercile bucket of family H's tick_volume z-score, consumed via the merged `upstream` dict (upstream['tick_volume_zscore_60']) rather than recomputed, which would duplicate family H",
  "source_module": "features.regime_state.compute_regime_state",
  "required_state": [
    "tick_volume"
  ],
  "update_trigger": "M1_CLOSE",
  "window": 500,
  "causal": true,
  "live_compatible": true,
  "computational_cost": "LOW",
  "numerical_stability_notes": "causal_tercile's rolling quantile thresholds are ill-defined (NaN) until min_periods=window//4=125 is satisfied; also inherits tick_volume_zscore_60's own NaN (first 60 bars, and any bar where its rolling std is exactly 0)",
  "missing_value_policy": "NaN for the first ~125 bars (window//4 min_periods gate on the rolling quantile), plus whatever NaN tick_volume_zscore_60 itself carries; never zero-filled",
  "warmup_bars": 125,
  "dependencies": [
    "jump_detection",
    "distribution_info",
    "microstructure_history"
  ],
  "historical_coverage": "PARTIAL_HISTORY",
  "status": "REDUNDANT",
  "status_reason": "Not a v3 survivor (research/output/v3_feature_survivors.json): catboost_importance=0.122 (<0.3), MI rank 57 (outside top 15). catboost_importance=0.122, cv_importance=0.421, mi_rank=57, family=I_regime_state, family_ablation_delta=-0.0006 (<0.1pp). Kept registered per spec section 9 -- Phase 4 may find it useful for a different target. Inherits PARTIAL_HISTORY from its upstream tick_volume_zscore_60 (family H): research/historical_coverage.py's measure_coverage found tick_volume_degrades_after='2020-09-28' against the real merged CSV.",
  "evidence_ref": "research/output/v3_feature_survivors.json; research/historical_coverage.py",
  "version": "v1"
}
