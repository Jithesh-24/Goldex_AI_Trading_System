__pycache__/
*.pyc

# Data matrices & raw feeds (multi-GB — live on the trading box, not in git)
*.csv
_sorted*
*.full*

# Logs
*.log

# Large model weights (regenerated by training; only JSON manifests are committed)
scripts/models/*.txt
scripts/models/*.npy

# Secrets — never commit
.env
*.pem